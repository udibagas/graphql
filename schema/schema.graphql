type User {
  _id: ID
  name: String
  email: String
  role: String
}

type Product {
  _id: ID
  name: String
  price: Int
  stock: Int
}

type Order {
  _id: ID
  customerId: String
  productId: String
  customer: User
  product: Product
  qty: Int
  amount: Int
  status: String
}
